# Load vendor_boot recovery modules in dump order, then extras.

on early-boot
    exec u:r:recovery:s0 root root -- /system/bin/sh -c "while read m; do [ -f /lib/modules/\$m ] && insmod /lib/modules/\$m 2>/dev/null; done < /lib/modules/modules.load.recovery"
